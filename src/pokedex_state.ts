import { defineStore } from "pinia";
import { ref, computed } from "vue";

export const dex_state = defineStore('dex_state', ()=> {
    const DISP_BUFFER_MAX = 32

    let dex_index: number = 0
    let disp_index: number = 0



    return { }
})